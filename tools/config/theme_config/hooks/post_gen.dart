import 'package:mason/mason.dart';

Future<void> run(HookContext context) async {
  context.logger.info('Copy this code in your MaterialApp widget:');
  context.logger.info('theme: CustomThemeData.themeLight(context),');
}
